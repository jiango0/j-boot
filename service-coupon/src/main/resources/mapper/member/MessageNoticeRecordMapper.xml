<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jzc.spring.coupon.dao.member.MessageNoticeRecordMapper" >

  <sql id="Base_Column_List" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Fri Oct 12 10:26:13 CST 2018.
    -->
    id, partner_sign, shop_guid, order_code, card_number, type, status, content, error_message,
    retry_num, flow_guid, create_time, update_time
  </sql>

  <select id="selectMessageNoticeRecord" resultType="com.jzc.spring.coupon.model.MessageNoticeRecord">
    select
    <include refid="Base_Column_List"/>
    from message_notice_record
    where partner_sign = ''
    order by id ASC
    limit ${pageIndex}, ${pageSize}
  </select>

  <select id="detail" resultType="com.jzc.spring.coupon.model.MessageNoticeRecord">
    select
    <include refid="Base_Column_List"/>
    from message_notice_record
    where id = #{id}
  </select>

  <update id="updateMessage" parameterType="com.jzc.spring.coupon.model.MessageNoticeRecord">
    update message_notice_record set
    status = 1,
    error_message = 'success'
    where id = {id}
  </update>

</mapper>