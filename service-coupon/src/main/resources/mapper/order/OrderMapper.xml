<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jzc.spring.coupon.dao.order.OrderMapper">

    <select id="selectOrders" resultType="com.jzc.spring.coupon.model.Order">
        select *
        from orders
        where order_code in
        <foreach collection="ids" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <select id="selectOrderItem" resultType="com.jzc.spring.coupon.model.OrderItem">
        select *
        from order_item
        where order_code in
        <foreach collection="ids" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

</mapper>